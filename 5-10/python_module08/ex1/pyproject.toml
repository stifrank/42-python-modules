[tool.poetry]
package-mode = false
name = "matrix-loading"
version = "0.1.0"
description = "Matrix data analysis dependency management exercise"
authors = ["fjaramil"]

[tool.poetry.dependencies]
python = "^3.10"
numpy = "*"
pandas = "*"
matplotlib = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"